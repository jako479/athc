; athc settings -- edit values to taste.
; Sections with no entry below use sensible defaults.
;
; No tools ship configurable settings yet. Each tool gets its own
; [section] here as tools are added.
